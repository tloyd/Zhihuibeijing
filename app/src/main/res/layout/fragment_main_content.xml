<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:layout_width="match_parent"
              android:layout_height="match_parent"
              android:orientation="vertical">

    <cc.springwind.zhihuibeijing.view.NoScrollViewPager
        android:id="@+id/vp_content_fmc"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"/>

    <RadioGroup
        android:id="@+id/rg_tab"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/bottom_tab_bg"
        android:gravity="center"
        android:orientation="horizontal">

        <RadioButton
            android:id="@+id/rb_home_fmc"
            style="@style/BottomTabStyle"
            android:checked="true"
            android:drawableTop="@drawable/tab_home_selector"
            android:text="首页"
            />

        <RadioButton
            android:id="@+id/rb_news_fmc"
            style="@style/BottomTabStyle"
            android:drawableTop="@drawable/tab_news_selector"
            android:text="新闻"
            />

        <RadioButton
            android:id="@+id/rb_service_fmc"
            style="@style/BottomTabStyle"
            android:drawableTop="@drawable/tab_service_selector"
            android:text="服务"
            />

        <RadioButton
            android:id="@+id/rb_gov_fmc"
            style="@style/BottomTabStyle"
            android:drawableTop="@drawable/tab_gov_selector"
            android:text="政务"
            />

        <RadioButton
            android:id="@+id/rb_setting_fmc"
            style="@style/BottomTabStyle"
            android:drawableTop="@drawable/tab_setting_selector"
            android:text="设置"
            />
    </RadioGroup>
</LinearLayout>